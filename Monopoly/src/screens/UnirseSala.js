import React from 'react';
import { View, Text } from'react-native';

export default function UnirseSalaScreen({ navigation }) {

    return (
        <View>
            <Text>Unirse Sala Screen</Text>
        </View>
    );
}