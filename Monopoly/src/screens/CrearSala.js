import React from "react";
import { View, Text } from "react-native";

export default function CrearSalaScreen({ navigation }) {

    return (
        <View>
            <Text>Crear Sala Screen</Text>
        </View>
    );
}